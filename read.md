AI Terminolgy
LLM, Agent,MCP

Large Language Models
chat bot (ChatGPT, Gemini, deepseek...)

Limitations
it can write an testcases but can't execute them

Can't do
Open browse or click buttons
connect to Database
call api and get reponse


Agnets 
Agent is a system or program that takes instructions from an LLM nad perofrms real-world task using external tools.
It acts as a helper that "does" things the LLM can't do on its own

User -> prompt -> LLM -> Agnet -> MCP

MCP(MOdel CONTEXT PROTOCAL)
is a framework that connects LLM to real-world tools(browsers, datBases, apis)
It allows LLMs to send commands to these tools and get results
Thinko of MCP as a bridge between the LLM and your computer/tools

 Tests mcp -> commands -> browser

 MCP with copilot
 

1.open "http://www.automationpractice.pl/index.php"
2. click on TSHirts
3. Select size as s
4. Click on first result